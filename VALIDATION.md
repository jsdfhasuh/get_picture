# 实现验证记录

验证日期：2026-09-19。环境：Windows 64 位、Python 3.10.21；
PySide6 6.11.2、NumPy 2.2.6、Pillow 12.3.0、pytest 9.1.1。

## 已验证

`pytest -q`：**18 passed**；`pip check`：依赖一致；PowerShell 启动脚本通过语法检查。

- 模拟 SDK：打开不覆盖曝光增益；带行填充的 BGR 图像正确转 RGB；
  SDK 缓冲释放后图像仍有效；无效帧及转换失败正确释放；
  曝光增益范围检查、写入回读失败停采、整数增益步长；
  正常断开恢复触发模式与触发选择器并释放句柄。
- 实际图片编码：ROI 尺寸和像素、PNG 无损、JPEG 4:4:4 与 RGB 色序、
  黑白 JPEG 保持单通道、中文路径、不覆盖重名文件和失败清理。
- Qt 离屏端到端：连接模拟相机、预览、反向拖动和移动 ROI、单张 PNG、
  定时采图、同帧压缩比较、参数应用、配置保存和线程关闭。
- 异常场景：慢编码时预览继续更新；队列满暂停定时保存；
  关闭时等待已入队图片完成；磁盘失败取消剩余排队任务并可重试；
  SDK 缺失不会退出界面；断开后不能保存旧预览。
- 已生成并目视检查中文主界面和比较窗口截图。

## 体积示例

使用同一张人工生成的 1920×1080 彩色图片（包含纹理、噪声、文字和边缘）：

| 格式 | 完整图像 | 780×480 ROI |
| --- | ---: | ---: |
| BMP | 6,220,854 字节 | 1,123,254 字节 |
| PNG | 3,689,458 字节 | 437,911 字节 |
| JPEG Q90 | 787,004 字节 | 121,791 字节 |
| JPEG Q95 | 1,499,585 字节 | 212,397 字节 |
| JPEG Q100 | 3,292,335 字节 | 440,074 字节 |

这些数值仅说明该样例的结果，不保证实际相机画面达到相同比例。
对于大面积纯色的模拟面板图，PNG 反而小于 JPEG，因此界面提供同帧比较。

## 尚未验证

默认路径和环境变量未找到已安装的 IMV SDK Runtime，本次没有连接实际相机。
真实型号的驱动、设备枚举、GigE/USB 传输、曝光增益范围、Bayer/高位深转换、
掉线恢复与触发配置恢复，仍需按照 README 的真机步骤验收。
模拟测试不能替代真机验证。本次未打包 EXE。

## 复现

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe scripts\verify_demo.py
```

截图、示例图片及 JSON 报告位于 `build/verification`，属于可重新生成的本地验证产物，
不上传到源码仓库。
