[project]
name = "get-picture"
version = "0.1.0"
description = "Dahua / Huaray camera capture with ROI and compact image storage"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
